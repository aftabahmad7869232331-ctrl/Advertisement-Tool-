import type { ViewType } from "../../components/Sidebar";

export const homeTool = {
  id: "home",
  label: "Home",
  view: "dashboard" as ViewType,
  description: "Dashboard home page with hero, stats, categories, recent projects, and campaign CTA.",
};
